import styled from 'styled-components';
import { Container, } from 'semantic-ui-react';

export const FlashMessage = styled(Container)`
  left: 0px;
  position: absolute;
  right: 0px;
  top: 20px;
  z-index: 100;
`